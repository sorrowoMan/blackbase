# 三仓发布协议

`blackbase`、`nsgablack` 与 `mlblack` 是一个按依赖方向发布的框架栈，不能把三次
独立打标签误认为一次完整发布。

当前发布顺序固定为：

1. `blackbase 0.3.8`
2. `nsgablack 0.3.9`，要求 `blackbase>=0.3.8,<0.4.0`
3. `mlblack 0.4.6`，要求 `blackbase>=0.3.8,<0.4.0` 与
   `nsgablack>=0.3.9,<0.4.0`

每一层只有在以下条件全部成立后才能发布下一层：

- 工作树已冻结，新增、删除和未跟踪文件都被明确纳入或排除。
- 本仓全量测试、Doctor（若提供）与 wheel 构建成功。
- wheel 在不依赖源码检出目录的隔离环境中可以导入。
- 当前层的发布产物已经能被下一层的依赖解析器取得。
- Git tag、包内版本、依赖下限和共享协议文档一致。

禁止先发布 `mlblack` 再等待底层版本补齐；这种顺序会生成元数据合法、但无法在
干净环境安装的发行包。若三仓任一层回滚，所有尚未发布的下游层都必须重新验证。
